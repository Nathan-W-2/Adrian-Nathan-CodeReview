#pragma once
#include <string>
using namespace std;
class InformalCodeReview
{
public:
	bool characteristic(const char numString[], int& c);
	bool mantissa(const char numString[], int& numerator, int& denominator);
};